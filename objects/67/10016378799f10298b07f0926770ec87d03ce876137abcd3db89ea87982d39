<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}OlympusRepo{% endblock %}</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
            background: #0d1117;
            color: #c9d1d9;
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        a { color: #58a6ff; text-decoration: none; }
        a:hover { text-decoration: underline; }

        /* NAV */
        nav {
            background: #161b22;
            border-bottom: 1px solid #30363d;
            padding: 12px 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 12px;
        }

        nav .logo {
            font-size: 1.2em;
            font-weight: 700;
            color: #f0f6fc;
        }

        nav .logo:hover { text-decoration: none; }
        nav .logo span { color: #ffd700; }

        nav .nav-links { display: flex; gap: 16px; align-items: center; flex-wrap: wrap; }

        nav .nav-links a {
            color: #8b949e;
            font-size: 0.9em;
            padding: 6px 12px;
            border-radius: 6px;
        }

        nav .nav-links a:hover {
            color: #f0f6fc;
            background: #21262d;
            text-decoration: none;
        }

        nav .user-badge {
            background: #21262d;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            color: #c9d1d9;
        }

        nav .role-tag {
            font-size: 0.75em;
            padding: 2px 6px;
            border-radius: 4px;
            margin-left: 6px;
            font-weight: 600;
        }

        .role-zeus { background: #ffd700; color: #000; }
        .role-olympian { background: #6e40c9; color: #fff; }
        .role-titan { background: #1f6feb; color: #fff; }
        .role-mortal { background: #30363d; color: #8b949e; }
        .role-prometheus { background: #f85149; color: #fff; }
        .role-hermes { background: #3fb950; color: #fff; }

        /* MAIN CONTENT */
        main { flex: 1; }

        .container {
            max-width: 960px;
            margin: 0 auto;
            padding: 32px 24px;
        }

        .container-wide {
            max-width: 1200px;
            margin: 0 auto;
            padding: 32px 24px;
        }

        h1 { font-size: 1.6em; color: #f0f6fc; margin-bottom: 16px; }
        h2 { font-size: 1.3em; color: #f0f6fc; margin-bottom: 12px; margin-top: 24px; }
        h3 { font-size: 1.1em; color: #c9d1d9; margin-bottom: 8px; }

        p { margin-bottom: 12px; }

        /* CARDS */
        .card {
            background: #161b22;
            border: 1px solid #30363d;
            border-radius: 6px;
            padding: 16px;
            margin-bottom: 12px;
        }

        .card:hover { border-color: #58a6ff; }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            gap: 12px;
            flex-wrap: wrap;
        }

        .card-title { font-size: 1.1em; font-weight: 600; color: #58a6ff; }

        .card-meta {
            font-size: 0.85em;
            color: #8b949e;
        }

        /* BADGES */
        .badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 0.75em;
            font-weight: 600;
        }

        .badge-public { background: #238636; color: #fff; }
        .badge-private { background: #da3633; color: #fff; }
        .badge-internal { background: #9e6a03; color: #fff; }

        /* TABLES */
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 12px 0;
        }

        th {
            text-align: left;
            padding: 8px 12px;
            border-bottom: 2px solid #30363d;
            color: #8b949e;
            font-size: 0.85em;
            font-weight: 600;
        }

        td {
            padding: 8px 12px;
            border-bottom: 1px solid #21262d;
            vertical-align: top;
        }

        tr:hover td { background: #161b22; }

        /* FORMS */
        .form-group { margin-bottom: 16px; }

        .form-group label {
            display: block;
            margin-bottom: 4px;
            font-size: 0.9em;
            color: #c9d1d9;
        }

        input[type="text"], input[type="password"], input[type="email"], textarea, select {
            width: 100%;
            padding: 8px 12px;
            background: #0d1117;
            border: 1px solid #30363d;
            border-radius: 6px;
            color: #c9d1d9;
            font-size: 0.95em;
            font-family: inherit;
        }

        input:focus, textarea:focus, select:focus {
            outline: none;
            border-color: #58a6ff;
        }

        textarea { min-height: 80px; resize: vertical; }

        .btn {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 6px;
            border: 1px solid #30363d;
            background: #21262d;
            color: #c9d1d9;
            font-size: 0.9em;
            cursor: pointer;
            font-weight: 600;
            font-family: inherit;
        }

        .btn:hover { background: #30363d; text-decoration: none; }
        .btn:disabled { opacity: 0.5; cursor: not-allowed; }
        .btn-primary { background: #238636; border-color: #238636; color: #fff; }
        .btn-primary:hover { background: #2ea043; }
        .btn-danger { background: #da3633; border-color: #da3633; color: #fff; }
        .btn-danger:hover { background: #f85149; }
        .btn-gold { background: #9e6a03; border-color: #ffd700; color: #ffd700; }
        .btn-gold:hover { background: #bb8009; }

        /* CODE / MONO */
        code, .mono {
            font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace;
            font-size: 0.9em;
        }

        .hash { color: #8b949e; font-family: monospace; font-size: 0.85em; }

        /* DIFF */
        .diff-add { color: #3fb950; }
        .diff-remove { color: #f85149; }
        .diff-stats { font-size: 0.85em; color: #8b949e; }

        /* EMPTY STATE */
        .empty-state {
            text-align: center;
            padding: 48px;
            color: #8b949e;
        }

        .empty-state p { font-size: 1.1em; }

        /* FOOTER */
        footer {
            border-top: 1px solid #21262d;
            padding: 24px;
            text-align: center;
            color: #484f58;
            font-size: 0.8em;
        }

        /* FLASH MESSAGES */
        .flash { padding: 12px 16px; border-radius: 6px; margin-bottom: 16px; }
        .flash-error { background: #3d1214; border: 1px solid #f85149; color: #f85149; }
        .flash-success { background: #12261e; border: 1px solid #3fb950; color: #3fb950; }

        /* UTILITY */
        .row { display: flex; gap: 12px; align-items: center; flex-wrap: wrap; }
        .row-end { display: flex; gap: 12px; align-items: center; justify-content: flex-end; flex-wrap: wrap; }
        .row-between { display: flex; gap: 12px; align-items: center; justify-content: space-between; flex-wrap: wrap; }
    </style>
    {% block extra_css %}{% endblock %}
</head>
<body>
    <nav>
        <a href="/login" class="logo"><span>Olympus</span>Repo</a>
        <div class="nav-links">
            <a href="/">Repositories</a>
            {% if user %}
                {% if user.role in ('zeus', 'olympian') %}
                    <a href="/zeus">The Throne</a>
                {% endif %}
                <a href="/messages" class="nav-links" id="messages-nav-link"
                   style="color:#8b949e; font-size:0.9em; padding:6px 12px; position:relative;">
                    Messages
                    <span id="msg-unread-badge" style="display:none; position:absolute;
                          top:2px; right:2px; background:#ffd700; color:#000;
                          border-radius:8px; font-size:0.65em; padding:1px 5px;
                          font-weight:700; min-width:16px; text-align:center;"></span>
                </a>
                <span class="user-badge">
                    {{ user.username }}
                    <span class="role-tag role-{{ user.role }}">{{ user.role }}</span>
                </span>
                <div id="notif-wrapper" style="position:relative;">
                    <button id="notif-btn" class="btn"
                            style="padding:6px 10px; position:relative; background:none; border:none;">
                        &#x1F514;
                        <span id="notif-badge" style="display:none; position:absolute; top:2px; right:2px;
                              background:#f85149; color:#fff; border-radius:8px; font-size:0.65em;
                              padding:1px 5px; font-weight:700;"></span>
                    </button>
                    <div id="notif-dropdown" style="display:none; position:absolute; right:0; top:38px;
                         width:320px; background:#161b22; border:1px solid #30363d; border-radius:6px;
                         z-index:100; max-height:400px; overflow-y:auto;">
                        <div style="padding:10px 14px; border-bottom:1px solid #30363d;
                                    display:flex; justify-content:space-between; align-items:center;">
                            <strong>Notifications</strong>
                            <a href="#" id="mark-all-read" style="font-size:0.8em;">Mark all read</a>
                        </div>
                        <div id="notif-list" style="padding:8px 0;"></div>
                    </div>
                </div>
                <a href="/logout" id="logout-link">Logout</a>
            {% else %}
                <a href="/login">Login</a>
            {% endif %}
        </div>
    </nav>

    <main>
        {% block content %}{% endblock %}
    </main>

    <footer>
        OlympusRepo &middot; Sovereign Version Control
        &middot; <a href="https://github.com/YOUR_USERNAME/olympusrepo" style="color:#484f58;">GitHub</a>
        &middot; MIT License
        &middot; The Thunderer watches all.
    </footer>

    <script>
        (function() {
            var btn = document.getElementById('notif-btn');
            var dropdown = document.getElementById('notif-dropdown');
            var badge = document.getElementById('notif-badge');
            var list = document.getElementById('notif-list');
            if (!btn) return;

            async function loadNotifications() {
                try {
                    var res = await fetch('/api/notifications');
                    if (!res.ok) return;
                    var data = await res.json();
                    if (data.unread > 0) {
                        badge.textContent = data.unread;
                        badge.style.display = 'inline';
                    } else {
                        badge.style.display = 'none';
                    }
                    list.innerHTML = '';
                    if (data.notifications.length === 0) {
                        list.innerHTML = '<div style="padding:12px 14px; color:#8b949e; font-size:0.9em;">No notifications.</div>';
                        return;
                    }
                    data.notifications.forEach(function(n) {
                        var div = document.createElement('div');
                        div.style.cssText = 'padding:10px 14px; border-bottom:1px solid #21262d; cursor:pointer;' +
                            (n.is_read ? 'opacity:0.6;' : 'background:#0d1117;');
                        div.innerHTML =
                            '<div style="font-size:0.85em; color:#c9d1d9;">' + n.message + '</div>' +
                            '<div style="font-size:0.75em; color:#8b949e; margin-top:2px;">' +
                            new Date(n.created_at).toLocaleDateString() + '</div>';
                        div.addEventListener('click', async function() {
                            await fetch('/api/notifications/' + n.notif_id + '/read', {method:'POST'});
                            if (n.link) window.location = n.link;
                            else loadNotifications();
                        });
                        list.appendChild(div);
                    });
                } catch(e) {}
            }

            btn.addEventListener('click', function(e) {
                e.stopPropagation();
                var isOpen = dropdown.style.display !== 'none';
                dropdown.style.display = isOpen ? 'none' : 'block';
                if (!isOpen) loadNotifications();
            });

            document.addEventListener('click', function() {
                dropdown.style.display = 'none';
            });

            document.getElementById('mark-all-read').addEventListener('click', async function(e) {
                e.preventDefault();
                await fetch('/api/notifications/read-all', {method:'POST'});
                loadNotifications();
            });

            // Poll every 60 seconds for new notifications
            loadNotifications();
            setInterval(loadNotifications, 300000);

            // Poll for unread messages
            var msgBadge = document.getElementById('msg-unread-badge');
            if (msgBadge) {
                async function loadUnreadMessages() {
                    try {
                        var res = await fetch('/api/messages/unread-count');
                        if (!res.ok) return;
                        var data = await res.json();
                        if (data.unread > 0) {
                            msgBadge.textContent = data.unread;
                            msgBadge.style.display = 'inline-block';
                        } else {
                            msgBadge.style.display = 'none';
                        }
                    } catch(e) {}
                }
                loadUnreadMessages();
                setInterval(loadUnreadMessages, 300000);
            }
        })();
    </script>

    {% block extra_js %}{% endblock %}
</body>
</html>
